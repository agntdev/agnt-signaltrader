[
  {
    "name": "Configure Channels",
    "steps": [
      {
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "No channels are monitored yet — tap Add channel to set one up."
            }
          }
        ],
        "send": {
          "callback": "config:channels"
        }
      }
    ]
  }
]
