[
  {
    "name": "Manage Posters",
    "steps": [
      {
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "No approved posters yet — tap Approve poster to add one."
            }
          }
        ],
        "send": {
          "callback": "config:posters"
        }
      }
    ]
  }
]
